<Window x:Class="WPF_OpenStreetmap_Editor.Views.OsmFeatureMetadataWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:services="clr-namespace:WPF_OpenStreetmap_Editor.Services"
        Title="{services:Loc Osm.Metadata.Title}" Height="420" Width="560"
        MinHeight="360" MinWidth="500"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Theme.WindowBrush}"
        Foreground="{DynamicResource Theme.TextBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="FeatureTextBlock" Grid.Row="0" FontWeight="SemiBold" TextWrapping="Wrap" />
        <Grid Grid.Row="1" Margin="0,12,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="110" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0" Text="{services:Loc Osm.Metadata.PrimitiveType}" Margin="0,0,8,8" VerticalAlignment="Center" />
            <ComboBox x:Name="PrimitiveTypeComboBox" Grid.Row="0" Grid.Column="1" Margin="0,0,0,8" />
            <TextBlock Grid.Row="1" Grid.Column="0" Text="OSM ID" Margin="0,0,8,8" VerticalAlignment="Center" />
            <TextBox x:Name="OsmIdTextBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,8" />
            <TextBlock Grid.Row="2" Grid.Column="0" Text="{services:Loc Osm.Metadata.Version}" Margin="0,0,8,0" VerticalAlignment="Center" />
            <TextBox x:Name="VersionTextBox" Grid.Row="2" Grid.Column="1" />
        </Grid>
        <TextBlock Grid.Row="2"
                   Margin="0,14,0,4"
                   Text="{services:Loc Osm.Metadata.NodeReferencesHelp}"
                   TextWrapping="Wrap" />
        <TextBox x:Name="NodeReferencesTextBox"
                 Grid.Row="3"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 TextWrapping="NoWrap" />
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="{services:Loc Osm.Metadata.ClearBinding}" Click="Clear_Click" Margin="0,0,6,0" />
            <Button Content="{services:Loc Common.Save}" IsDefault="True" Click="Save_Click" Margin="0,0,6,0" />
            <Button Content="{services:Loc Common.Cancel}" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
