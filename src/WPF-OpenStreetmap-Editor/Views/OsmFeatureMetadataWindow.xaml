<Window x:Class="WPF_OpenStreetmap_Editor.Views.OsmFeatureMetadataWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="编辑原始 OSM 元数据" Height="420" Width="560"
        MinHeight="360" MinWidth="500"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Theme.WindowBrush}"
        Foreground="{DynamicResource Theme.TextBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="FeatureTextBlock" Grid.Row="0" FontWeight="SemiBold" TextWrapping="Wrap" />
        <Grid Grid.Row="1" Margin="0,12,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="110" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Row="0" Grid.Column="0" Text="OSM 类型" Margin="0,0,8,8" VerticalAlignment="Center" />
            <ComboBox x:Name="PrimitiveTypeComboBox" Grid.Row="0" Grid.Column="1" Margin="0,0,0,8" />
            <TextBlock Grid.Row="1" Grid.Column="0" Text="OSM ID" Margin="0,0,8,8" VerticalAlignment="Center" />
            <TextBox x:Name="OsmIdTextBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,8" />
            <TextBlock Grid.Row="2" Grid.Column="0" Text="版本号" Margin="0,0,8,0" VerticalAlignment="Center" />
            <TextBox x:Name="VersionTextBox" Grid.Row="2" Grid.Column="1" />
        </Grid>
        <TextBlock Grid.Row="2"
                   Margin="0,14,0,4"
                   Text="Way 节点引用（每行一个：nodeId 或 nodeId:version；留空则按当前几何创建新节点）" 
                   TextWrapping="Wrap" />
        <TextBox x:Name="NodeReferencesTextBox"
                 Grid.Row="3"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 TextWrapping="NoWrap" />
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="清除绑定" Click="Clear_Click" Margin="0,0,6,0" />
            <Button Content="保存" IsDefault="True" Click="Save_Click" Margin="0,0,6,0" />
            <Button Content="取消" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
