<Window x:Class="WPF_OpenStreetmap_Editor.Views.PluginsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="插件" Height="570" Width="760"
        MinHeight="360" MinWidth="620"
        Background="{DynamicResource Theme.WindowBackgroundBrush}"
        Foreground="{DynamicResource Theme.TextBrush}"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Margin="0,0,0,10"
                   Text="原生 DLL 插件在主进程内运行；文件变化后必须重新确认。进程插件在 AppContainer 中隔离。"
                   TextWrapping="Wrap" />

        <DataGrid x:Name="PluginsDataGrid"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionChanged="PluginsDataGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="名称" Binding="{Binding Name}" Width="2*" />
                <DataGridTextColumn Header="版本" Binding="{Binding Version}" Width="*" />
                <DataGridTextColumn Header="类型" Binding="{Binding Kind}" Width="*" />
                <DataGridTextColumn Header="状态" Binding="{Binding StatusText}" Width="2*" />
            </DataGrid.Columns>
        </DataGrid>

        <Grid x:Name="PluginDetailsPanel"
              Grid.Row="2"
              Margin="0,12,0,0"
              MinHeight="92"
              Visibility="Collapsed">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Border Width="72"
                    Height="72"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    BorderThickness="1"
                    BorderBrush="{DynamicResource Theme.BorderBrush}">
                <Image x:Name="PluginIconImage"
                       Width="64"
                       Height="64"
                       Stretch="Uniform" />
            </Border>
            <StackPanel Grid.Column="1" Margin="8,0,0,0">
                <TextBlock x:Name="PluginDetailsTitle"
                           FontSize="15"
                           FontWeight="SemiBold"
                           TextWrapping="Wrap" />
                <ScrollViewer Margin="0,6,0,0"
                              MaxHeight="76"
                              VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="PluginDetailsDescription"
                               TextWrapping="Wrap" />
                </ScrollViewer>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="3" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="安装..." Width="88" Margin="0,0,8,0" Click="Install_Click" />
            <Button x:Name="TrustButton"
                    Grid.Column="1"
                    Content="信任并加载"
                    Width="104"
                    Margin="0,0,8,0"
                    IsEnabled="False"
                    Click="Trust_Click" />
            <Button Grid.Column="2" Content="重新扫描" Width="88" Click="Reload_Click" />
            <Button Grid.Column="4" Content="关闭" Width="88" IsCancel="True" />
        </Grid>
    </Grid>
</Window>
