<Window x:Class="WPF_OpenStreetmap_Editor.Views.OsmUploadWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="上传到 OpenStreetMap" Height="310" Width="560"
        MinHeight="280" MinWidth="500"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource Theme.WindowBrush}"
        Foreground="{DynamicResource Theme.TextBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="AccountTextBlock" Grid.Row="0" FontWeight="SemiBold" />
        <TextBlock x:Name="ChangeSummaryTextBlock" Grid.Row="1" Margin="0,8,0,12" />
        <TextBlock Grid.Row="2" Text="变更说明" Margin="0,0,0,4" />
        <TextBox x:Name="CommentTextBox" Grid.Row="3" AcceptsReturn="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto" MaxLength="255" />
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="上传" IsDefault="True" Click="Upload_Click" Margin="0,0,6,0" />
            <Button Content="取消" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
