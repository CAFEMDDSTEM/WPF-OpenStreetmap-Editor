<Window x:Class="WPF_OpenStreetmap_Editor.Views.DataValidatorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:services="clr-namespace:WPF_OpenStreetmap_Editor.Services"
        xmlns:iconPacks="clr-namespace:MahApps.Metro.IconPacks;assembly=MahApps.Metro.IconPacks.Lucide"
        Title="{services:Loc DataValidator.Title}"
        Height="640"
        Width="860"
        MinHeight="420"
        MinWidth="680"
        Background="{DynamicResource Theme.WindowBackgroundBrush}"
        Foreground="{DynamicResource Theme.TextBrush}"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="{services:Loc DataValidator.Title}" FontSize="18" FontWeight="SemiBold" />
                <TextBlock Text="{services:Loc DataValidator.Description}"
                           Margin="0,3,0,0"
                           TextWrapping="Wrap"
                           Foreground="{DynamicResource Theme.MutedTextBrush}" />
            </StackPanel>
            <iconPacks:PackIconLucide Grid.Column="1"
                                      Kind="ShieldCheck"
                                      Width="34"
                                      Height="34"
                                      Foreground="{DynamicResource Theme.AccentBrush}" />
        </Grid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="{services:Loc DataValidator.Run}" Padding="12,3" Click="Run_Click" />
            <Button Content="{services:Loc DataValidator.Locate}" Padding="12,3" Margin="6,0,0,0" Click="Locate_Click" />
            <Button Content="{services:Loc DataValidator.Fix}" Padding="12,3" Margin="6,0,0,0" Click="Fix_Click" />
        </StackPanel>

        <DataGrid x:Name="IssueDataGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  MouseDoubleClick="IssueDataGrid_MouseDoubleClick"
                  AutomationProperties.Name="{services:Loc DataValidator.Results}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="{services:Loc Common.Status}" Binding="{Binding SeverityText}" Width="100" />
                <DataGridTextColumn Header="{services:Loc DataValidator.Target}" Binding="{Binding TargetText}" Width="180" />
                <DataGridTextColumn Header="{services:Loc DataValidator.Message}" Binding="{Binding Message}" Width="*" />
                <DataGridTextColumn Header="{services:Loc DataValidator.Fix}" Binding="{Binding FixText}" Width="220" />
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="3" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="SummaryTextBlock"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource Theme.MutedTextBrush}"
                       AutomationProperties.LiveSetting="Polite" />
            <Button Grid.Column="1" Content="{services:Loc Common.Close}" Width="88" IsCancel="True" />
        </Grid>
    </Grid>
</Window>
