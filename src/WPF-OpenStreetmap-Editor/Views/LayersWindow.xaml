<Window x:Class="WPF_OpenStreetmap_Editor.Views.LayersWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:services="clr-namespace:WPF_OpenStreetmap_Editor.Services"
        Title="{services:Loc Layers.Title}" Height="420" Width="480"
        Background="{DynamicResource Theme.WindowBackgroundBrush}"
        Foreground="{DynamicResource Theme.TextBrush}">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,6">
            <TextBlock VerticalAlignment="Center" Margin="0,0,6,0" Text="{services:Loc Layers.Url}" />
            <TextBox x:Name="LayerUrlTextBox" Width="320" />
            <ComboBox x:Name="LayerTypeComboBox" Width="80" Margin="6,0,0,0" SelectedIndex="0">
                <ComboBoxItem>XYZ</ComboBoxItem>
                <ComboBoxItem>TMS</ComboBoxItem>
            </ComboBox>
        </StackPanel>

        <ListBox x:Name="LayersListBox" Grid.Row="1" />

        <StackPanel Orientation="Horizontal" Grid.Row="2" HorizontalAlignment="Right" Margin="0,6,0,0">
            <Button Content="{services:Loc Common.Add}" Width="80" Click="AddLayer_Click" Margin="0,0,6,0" />
            <Button Content="{services:Loc Layers.RemoveSelected}" Width="80" Click="RemoveLayer_Click" Margin="0,0,6,0" />
            <Button Content="{services:Loc Layers.LoadSelected}" Width="100" Click="LoadSelected_Click" />
        </StackPanel>
    </Grid>
</Window>
